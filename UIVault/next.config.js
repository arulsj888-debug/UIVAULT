/** @type {import('next').NextConfig} */
const nextConfig = {
  // Required for serving static template files from /public
  // Vercel handles this automatically
  experimental: {},
};

module.exports = nextConfig;
